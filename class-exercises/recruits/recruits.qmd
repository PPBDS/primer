---
title: "Wisdom and Recruits"
execute:
  echo: false
---

## Imagine

Imagine that you are a logistics analyst at U.S. Marine Corps Supply, planning next year's bootcamp uniform order. You want to use the available data to make a sensible forecast for the recruits who will arrive next year.

The data describe a sample of recruits with information such as height, sex, and age. Each row is one recruit, and the goal is to use what is known before the forecast to reason about what will happen next year.

## Wisdom

We are working with the `recruits` dataset. This exercise is a predictive modeling exercise. We will use it to reason about how to forecast the height of next year's recruits from information we already know about them.

What question might you try to answer if you are a logistics analyst planning next year's uniform order?

What are the units, outcome, and covariates in this problem?

- Unit:
- Outcome:
- Covariates:
- Treatment (if any):
- Quantity of interest:

What belongs in the conceptual Preceptor Table for this problem? You do not need to construct it yet.

The instructor will display the completed Preceptor Table after the discussion.

## Justice

Now that we know the question, we need to think about the population and the assumptions that justify using the data to answer it.

Describe the Population Table in your own words.

What is one realistic violation of validity or representativeness in this problem?

Why is unconfoundedness not part of this predictive modeling problem?

## Courage

Before we fit a model, we should understand what model we are using and why.

What outcome are we modeling? What predictors will we use?

Why is this model reasonable?

The instructor will show the fitted model and its output.

## Temperance

Now that the model is fitted, we need to interpret it carefully and turn the result into a short Quarto report.

What is the main conclusion from the fitted model?

This is the code Preceptor has prepared for you. It loads the data, prepares it, and fits the model so you can build on it before asking AI to create the plot.

```{r}
#| label: preceptor-code
#| message: false

library(tidyverse)
library(primer.tutorials)

data("recruits", package = "primer.tutorials")

recruits <- recruits |>
  mutate(sex = factor(sex, levels = c("Male", "Female")))

fit_recruits <- lm(height ~ sex, data = recruits)
summary(fit_recruits)
```

Ask AI to create a plot that summarizes the fitted model and the data. Use AI for the coding, and then write a short Quarto document that includes the plot and a brief interpretation. Publish the result to GitHub Pages.

If you finish early, ask AI to help you explore one of these ideas for the recruits data:

- suggest a better-looking plot with clearer labels, colors, or annotations for the height-by-sex relationship;
- identify any unusual recruits or outliers and explain whether they change the main conclusion;
- use functions such as `summarise()`, `group_by()`, `filter()`, or `arrange()` to examine the data more closely and then describe what you learn;
- ask AI to propose a few follow-up questions about the data that could shape a stronger final Quarto report.
